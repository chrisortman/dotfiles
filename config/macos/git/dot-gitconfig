[user]
	name = Chris Ortman
	email = chris-ortman@uiowa.edu
  signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMLIEVItMsiK40jwM2O0SPgqcish0TwXyr2xYp5bvREV

[gpg]
  format = ssh

[gpg "ssh"]
  program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"

[commit]
  gpgsign = true

[alias]
	st = status
	ci = commit
	co = checkout

  bird = log --graph --decorate --format=format:'%C(bold blue)%h%C(reset)%C(bold yellow)%d%C(reset): %s' 

[merge]
	tool = kdiff3NoAuto
[rerere]
	enabled = true
[core]
	excludesfile = ~/.gitignore_global
	trustctime = false
	attributesfile = ~/.gitattributes
	symlinks = true
[push]
	default = simple
	followTags = true
[mergetool "kdiff3NoAuto"]
	cmd = kdiff3 --L1 \"$MERGED (Base)\" --L2 \"$MERGED (Local)\" --L3 \"$MERGED (Remote)\" -o \"$MERGED\" \"$BASE\" \"$LOCAL\" \"$REMOTE\"

[color]
	ui = auto

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
# [pager]
# 	diff = delta
# 	log = delta
# 	reflog = delta
# 	show = delta

[delta]
    plus-style = "syntax #012800"
    minus-style = "syntax #340001"
    syntax-theme = Monokai Extended
    navigate = true

# [interactive]
#     diffFilter = delta --color-only
# [pager]
#   diff = diff-so-fancy | less --tabs=1,5 -RFX
#   show = diff-so-fancy | less --tabs=1,5 -RFX
[init]
	defaultBranch = main
[pull]
	ff = only
[mergetool "bc"]
	trustExitCode = true

