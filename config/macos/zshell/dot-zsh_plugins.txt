
# add completions functions to fpath
zsh-users/zsh-completions kind:fpath path:src

aloxaf/fzf-tab

# Completion styles are applied by `compstyle zshzoo` in .zshrc. The
# `path:completion/functions kind:autoload` bundle that used to live here was
# redundant -- completion.plugin.zsh adds that directory to fpath and runs
# compstyleinit itself -- and its generated `autoload -Uz $fpath[-1]/*` line
# broke when fzf-tab's lib directory ended up last in fpath.

# Keybindings
belak/zsh-utils path:editor

# History
belak/zsh-utils path:history

sindresorhus/pure     kind:fpath

# Utilities
zshzoo/macos conditional:is-macos
belak/zsh-utils path:utility
romkatv/zsh-bench kind:path
ohmyzsh/ohmyzsh path:plugins/extract

# Other Fish-like features
zdharma-continuum/fast-syntax-highlighting  # Syntax highlighting
zsh-users/zsh-autosuggestions               # Auto-suggestions
zsh-users/zsh-history-substring-search      # Up/Down to search history

#vi mode -- better than default?
jeffreytse/zsh-vi-mode

# run towards the BOTTOM of your .zsh_plugins.txt after fpath is populated
belak/zsh-utils path:completion
